version: "3.8"

services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: tiago_delivery
      POSTGRES_USER: tiago
      # AVISO: Esta senha é apenas para desenvolvimento local
      # Em produção, use variáveis de ambiente ou Docker secrets
      POSTGRES_PASSWORD: password123
    ports:
      - "5433:5432"
